apply plugin: 'java-library'
apply plugin: 'kotlin'

repositories {
    jcenter()
}

dependencies {
    compile deps.kotlin
    compile deps.cfuture
    compile deps.gson
    compile deps.okhttp

    compile project(':ljubase')
}
